== Entscheidungen mit IF

=== Das lernst du

In diesem Kapitel lernst du:

* wie Programme Entscheidungen treffen
* wie die Anweisung IF verwendet wird
* wie Werte miteinander verglichen werden
* wie Programme unterschiedlich auf Eingaben reagieren

Am Ende des Kapitels kannst du Programme schreiben, die auf Benutzereingaben reagieren und unterschiedliche Aktionen ausführen.

=== Warum Entscheidungen wichtig sind

Bisher wurden unsere Programme immer von oben nach unten ausgeführt.

Ein Programm soll aber oft unterschiedlich reagieren.

Zum Beispiel:

* Ist ein Benutzer volljährig?
* Wurde die richtige Antwort eingegeben?
* Ist eine Zahl größer als 100?

Für solche Aufgaben verwendet man die Anweisung `IF`.

=== Unsere erste Entscheidung

Gib folgendes Programm ein:

[source,basic]
----
A=10

IF A=10 THEN
  PRINT "Richtig!"
ENDIF
----

Ausgabe:

[source]
----
Richtig!
----

Die Bedingung ist erfüllt.

Deshalb wird die Anweisung zwischen `THEN` und `ENDIF` ausgeführt.

=== Wenn die Bedingung nicht erfüllt ist

Ändere das Programm:

[source,basic]
----
A=5

IF A=10 THEN
  PRINT "Richtig!"
ENDIF
----

Ausgabe:

[source]
----
----

Es erscheint keine Ausgabe.

Die Bedingung ist nicht erfüllt.

Deshalb wird der Bereich zwischen THEN und ENDIF übersprungen.

=== ELSE - Was passiert sonst?

Oft soll ein Programm nicht nur auf richtige Eingaben reagieren.

Es soll auch etwas tun, wenn die Bedingung nicht erfüllt ist.

Dafür gibt es ELSE.

[source,basic]
----
A=5

IF A=10 THEN
PRINT "Richtig!"
ELSE
PRINT "Falsch!"
ENDIF
----

Ausgabe:

[source]
----
Falsch!
----

Ändere nun den Wert von A auf 10.

Welche Ausgabe erscheint jetzt?

=== Werte vergleichen

Mit IF können verschiedene Vergleiche durchgeführt werden.

Gleich:

[source,basic]
----
IF A=10 THEN
PRINT "Gleich"
ENDIF
----

Ungleich:

[source,basic]
----
IF A<>10 THEN
PRINT "Ungleich"
ENDIF
----

Größer:

[source,basic]
----
IF A>10 THEN
PRINT "Groesser"
ENDIF
----

Kleiner:

[source,basic]
----
IF A<10 THEN
PRINT "Kleiner"
ENDIF
----

Größer oder gleich:

[source,basic]
----
IF A>=10 THEN
PRINT "Groesser oder gleich"
ENDIF
----

Kleiner oder gleich:

[source,basic]
----
IF A<=10 THEN
PRINT "Kleiner oder gleich"
ENDIF
----

=== Alter prüfen

Mit Benutzereingaben werden Entscheidungen besonders interessant.

[source,basic]
----
PRINT "Wie alt bist du?"
INPUT ALTER

IF ALTER>=18 THEN
PRINT "Du bist volljaehrig."
ELSE
PRINT "Du bist minderjaehrig."
ENDIF
----

Beispiel:

[source]
----
Wie alt bist du?
? 25

Du bist volljaehrig.
----

=== Texte vergleichen

Auch Texte können verglichen werden.

[source,basic]
----
PRINT "Wie heisst du?"
INPUT NAME$

IF NAME$="MANFRED" THEN
PRINT "Hallo Manfred!"
ELSE
PRINT "Hallo unbekannter Besucher!"
ENDIF
----

=== Unser erstes Quiz

[source,basic]
----
PRINT "Wieviel ist 2+3?"
INPUT ANTWORT

IF ANTWORT=5 THEN
PRINT "Richtig!"
ELSE
PRINT "Leider falsch."
ENDIF
----

Beispiel:

[source]
----
Wieviel ist 2+3?
? 5

Richtig!
----

=== Mehrere Entscheidungen

Ein Programm kann beliebig viele Entscheidungen enthalten.

[source,basic]
----
INPUT NOTE

IF NOTE=1 THEN
PRINT "Sehr gut"
ENDIF

IF NOTE=2 THEN
PRINT "Gut"
ENDIF

IF NOTE=3 THEN
PRINT "Befriedigend"
ENDIF
----

=== ELSEIF verwenden

Oft müssen mehrere Möglichkeiten geprüft werden.

Dafür eignet sich `ELSEIF`.

[source,basic]
----
INPUT NOTE

IF NOTE=1 THEN
  PRINT "Sehr gut"
ELSEIF NOTE=2 THEN
  PRINT "Gut"
ELSEIF NOTE=3 THEN
  PRINT "Befriedigend"
ELSE
  PRINT "Unbekannte Note"
ENDIF
----

Beispiel:

[source]
----
? 2

Gut
----

=== Experimentiere!

Probiere folgende Beispiele aus:

[source,basic]
----
INPUT ZAHL

IF ZAHL>100 THEN
PRINT "Die Zahl ist groesser als 100."
ELSE
PRINT "Die Zahl ist kleiner oder gleich 100."
ENDIF
----

Was passiert bei verschiedenen Eingaben?

=== Probier's selbst!

Versuche folgende Aufgaben:

. Frage das Alter ab und prüfe, ob die Person volljährig ist.
. Frage einen Namen ab und gib eine persönliche Begrüßung aus.
. Prüfe, ob eine Zahl größer als 100 ist.
. Erstelle ein kleines Rechenquiz.
. Erweitere das Quiz um eine Ausgabe für falsche Antworten.

=== Zusammenfassung

In diesem Kapitel hast du die Anweisung IF kennengelernt.

Du kannst nun:

Werte vergleichen
Entscheidungen treffen
auf Benutzereingaben reagieren
unterschiedliche Programmabläufe mit ELSE erstellen

Im nächsten Kapitel lernen wir Schleifen mit FOR ... NEXT kennen.